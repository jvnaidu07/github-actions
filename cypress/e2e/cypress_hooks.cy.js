/// <reference types = 'cypress'/>

describe('cypress types of hooks', ()=>{
    before(()=>{
        //it run before all test blocks
    });
    beforeEach(()=>{
        //it runs before each test block
    });
    after(()=>{
        // it runs after all tests block
    })
    afterEach(()=>{
        //it runs after each test block
    })


    specify("hooks in cypress", ()=>{

    })
})